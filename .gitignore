# Data — never commit. IEEE-CIS is ~1.35GB.
data/raw/
data/processed/
*.csv
!data/sample/*.csv

# Artifacts
artifacts/*.pkl
artifacts/*.joblib
# Reproducible from `make baseline` / `make train`. Metrics JSON IS committed.
evaluation/preds/*.parquet

# Secrets
.env

# Personal sprint notes — contains availability/interview material and this
# repo is public. Ignored rather than merely untracked, so `git add -A` cannot
# pull it in by accident.
SPRINT.md

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ipynb_checkpoints/

# Generated documentation — regenerate with `make guide`
docs/ChargeShield-Guide.pdf

# Editor / agent tooling — not part of the project
.claude/

# OS
.DS_Store

# Audit log — regenerated by `make batch`
evidence/audit_log.jsonl
