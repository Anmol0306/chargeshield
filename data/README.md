# Data

`raw/` and `processed/` are gitignored.

Download IEEE-CIS Fraud Detection from Kaggle into `raw/`:
  train_transaction.csv, train_identity.csv

Do not use test_transaction.csv — it has no labels.
